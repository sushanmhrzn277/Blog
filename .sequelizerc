const path=require('path');
const basePath=path.resolve('./services/mysql');

module.exports={
    "config":path.join(basePath,"/config/config.js"),
    "seeders-path":path.join(basePath,"/seeders"),
    "migrations-path":path.join(basePath,"/migrations")
}